<Window x:Class="AutoBrowserDownloader.WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoBrowserDownloader.WpfApp"
        mc:Ignorable="d"
        Title="Web Scraper" Height="700" Width="1100" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CBD5E0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2D3748"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3182CE"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="#F7FAFC">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#1A202C">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="20,30,20,40">
                     <Image Source="/WpfApp/Assets/logo.png" Width="32" Height="32" Margin="0,0,10,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                     <TextBlock Text="Web Scraper" Foreground="White" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>
                
                <Button Content="Project 1" Style="{StaticResource SidebarButtonStyle}" Background="#2D3748" Foreground="White" BorderBrush="#3182CE" BorderThickness="4,0,0,0"/>
                <Button Content="Scheduler" Style="{StaticResource SidebarButtonStyle}"/>
                <Button Content="Browser" Style="{StaticResource SidebarButtonStyle}"/>
                <Button Content="Settings" Style="{StaticResource SidebarButtonStyle}"/>
                <Button Content="About" Click="About_Click" Style="{StaticResource SidebarButtonStyle}"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Header & Controls -->
            <StackPanel Grid.Row="0" Margin="20" Background="White">
                <Border Background="White" CornerRadius="8" Padding="15">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" Direction="270" ShadowDepth="2" Opacity="0.1" Color="Black"/>
                    </Border.Effect>
                    <StackPanel>
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Target URL" Foreground="#718096" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox x:Name="UrlBox" Grid.Column="1" Text="https://www.dafontfree.io/" Padding="10,8" BorderBrush="#E2E8F0" BorderThickness="1"/>
                        </Grid>
                        
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="Start" Click="Start_Click" Style="{StaticResource ModernButtonStyle}" Background="#4299E1" Width="80"/>
                            <Button Content="Stop"  Margin="10,0,0,0" Style="{StaticResource ModernButtonStyle}" Background="#A0AEC0" Width="80"/>
                            
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="30,0,0,0">
                                <TextBlock Text="Delay (ms)" Foreground="#718096" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <TextBox Text="1000" Width="60" Padding="5" BorderBrush="#E2E8F0" Margin="0,0,15,0"/>
                                <TextBlock Text="Threads" Foreground="#718096" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <TextBox Text="3" Width="40" Padding="5" BorderBrush="#E2E8F0"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Main Dashboard Area -->
            <TabControl Grid.Row="1" Margin="20,0,20,20" BorderThickness="0" Background="Transparent" SelectedIndex="0">
                <TabItem Header="Dashboard" FontSize="14">
                    <Grid Margin="0,10,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="2*"/>
                            <RowDefinition Height="1*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Extracted Data -->
                        <Border Grid.Row="0" Grid.Column="0" Background="White" CornerRadius="8" Margin="0,0,10,10" Padding="15">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="Extracted Data" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                                <DataGrid Grid.Row="1" x:Name="ResultsGrid" AutoGenerateColumns="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal" Background="White" BorderThickness="0">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="No" Binding="{Binding No}" Width="40"/>
                                        <DataGridTextColumn Header="Name" Binding="{Binding FontName}" Width="*"/>
                                        <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="100"/>
                                        <DataGridTextColumn Header="Font URL" Binding="{Binding FontUrl}" Width="150"/>
                                        <DataGridTextColumn Header="DL URL" Binding="{Binding DownloadUrl}" Width="150"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Border>

                        <!-- Browser Preview -->
                        <Border Grid.Row="0" Grid.Column="1" Background="White" CornerRadius="8" Margin="10,0,0,10" Padding="15">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="Browser Preview" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                                <Border Grid.Row="1" Background="#F0F4F8" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="4">
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <TextBlock Text="Initial Browser Preview" Foreground="#A0AEC0" HorizontalAlignment="Center"/>
                                        <TextBlock Text="(Live View Placeholder)" Foreground="#CBD5E0" FontSize="12" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                    </StackPanel>
                                </Border>
                                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0" HorizontalAlignment="Right">
                                    <Button Content="Inspect" Style="{StaticResource ModernButtonStyle}" Background="#4299E1" FontSize="12" Padding="10,5"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Logs Section -->
                        <Border Grid.Row="1" Grid.ColumnSpan="2" Background="#2D3748" CornerRadius="8" Padding="10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                    <TextBlock Text="Status: " Foreground="#A0AEC0" FontWeight="Bold"/>
                                    <TextBlock Text="Idle" Foreground="White" FontWeight="Bold"/>
                                </StackPanel>
                                <TextBox Grid.Row="1" x:Name="LogBox" Background="Transparent" Foreground="#E2E8F0" BorderThickness="0" 
                                         IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12"/>
                            </Grid>
                        </Border>
                    </Grid>
                </TabItem>

                <TabItem Header="Selectors (Config)" FontSize="14">
                    <Grid Margin="0,10,0,0" Background="White">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Margin="10">
                            <Button Content="Add Step" Click="AddStep_Click" Style="{StaticResource ModernButtonStyle}" Background="#48BB78"/>
                        </StackPanel>
                        <DataGrid Grid.Row="1" x:Name="StepsGrid" AutoGenerateColumns="False" Margin="10" CanUserAddRows="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Type" Binding="{Binding Type}"/>
                                <DataGridTextColumn Header="Selector" Binding="{Binding Selector}" Width="*"/>
                                <DataGridTextColumn Header="Fallback Selector" Binding="{Binding FallbackSelector}" Width="*"/>
                                <DataGridTextColumn Header="Value (Input/Delay)" Binding="{Binding Value}" Width="100"/>
                                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="150"/>
                                <DataGridCheckBoxColumn Header="Optional" Binding="{Binding IsOptional}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</Window>
